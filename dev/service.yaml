
---
apiVersion: v1
kind: Service
metadata:
  name: web-app-service
spec:
  selector:
    app.kubernetes.io/name: web-app
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 8080
  type: LoadBalancer
