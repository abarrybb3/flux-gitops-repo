---
apiVersion: v1
kind: Service
metadata:
  name: web-app-service
spec:
  selector:
    app.kubernetes.io/name: webb-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
